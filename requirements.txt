# ----------------------------------------------------------------------
# PyTorch  --  install the CUDA wheel separately (see README).
# `pip install -r requirements.txt` would only get the CPU-only build
# from the default PyPI index, which is almost never what you want on
# a GPU machine.  Recommended install:
#
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch
#
# Pick a different CUDA build if your driver / local CUDA toolkit
# requires it: cu118 / cu121 / cu124 / cu126 / cu128 (cu128 is Linux-only).
# ----------------------------------------------------------------------
torch>=2.0.0
numpy>=1.22.0
scipy>=1.9.0
matplotlib>=3.5.0
cma>=3.2.0
ipykernel>=6.0.0
jupyter>=1.0.0
